let name: string = "String";

// the number type can represent integer and floating point numbers
// number range is: [-(2^53-1), 2^53-1]
let num: number = 123456;

const bigNum = 1234567890123456789012345678901234567890n;
console.log("number: {num}" );
console.log(bigNum);

// alert() is a property of browser `window` objects
// alert(1 / 0);
// alert(Infinity);

export {}
